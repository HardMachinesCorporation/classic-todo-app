// prettier.config.js (à la racine)
module.exports = {
    printWidth: 100,
    singleQuote: true,
    bracketSpacing: true,
    trailingComma: 'all',
    // on ne spécifie **pas** de overrides ici, on gère l’inclusion/exclusion via .prettierignore
};